# Fonts (bundled)

**General Sans** WOFF2 files used by `@font-face` in `src/index.css` (paths `url('./assets/fonts/…')` so Vite hashes them in production).

Mirrors the same files under repo root `fonts/` (see `fonts/README.md` for license and refresh notes).
